@model IEnumerable<ClassWarfare.Models.CountryListItem>

@{
                /**/

                ViewBag.Title = "Index";
}
<div class="tableindexheader">
    <div class="tabletitle">
        <h2>Add a Country</h2>
    </div>

    <div class="tableindexcreate">
        <p>
            @Html.ActionLink("Create New", "Create", null, new { style = "color: rgb(128,128,128)" })
        </p>
    </div>
</div>

@if (TempData.ContainsKey("CountrySaveMessage"))
{
    <div class="alert alert-success fade in" role="alert">
        @ViewBag.SaveResult

        <button type="button" class="close" data-dismiss="alert">
            <span>&times;</span>
        </button>
        @TempData["SaveResult"]
        @TempData["CountrySaveMessage"]
    </div>
}

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CountryId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CountryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CountryTech)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DipRelations)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StaffAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CountryBudget)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CountryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CountryName)
            </td>
            <td>
                @Html.DisplayFor(model => item.CountryTech)
            </td>
            <td>
                @Html.DisplayFor(model => item.DipRelations)
            </td>
            <td>
                @Html.DisplayFor(model => item.StaffAmount)
            </td>
            <td>
                @Html.DisplayFor(model => item.CountryBudget)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CountryId }) |
                @Html.ActionLink("Details", "Details", new { id = item.CountryId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.CountryId })
            </td>
        </tr>
    }

</table>
